{
  "name": "hub-05-education",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Education and student finance calculator hub. Hub 5 of the utility site portfolio.",
  "scripts": {
    "dev": "astro dev --port 3029",
    "build": "node scripts/check-data.mjs && astro build",
    "preview": "astro preview --port 3029",
    "astro": "astro",
    "test": "node --import ./scripts/alias-loader.mjs scripts/test-models.mjs",
    "data:check": "node scripts/check-data.mjs"
  },
  "dependencies": {
    "astro": "^5.6.1",
    "@fontsource-variable/inter": "^5.2.5"
  }
}
